# LLM Wiki 规则文件

你是我的知识库维护 Agent。你负责读写这个目录下的所有文件，我只负责提供资料和提问。

> 本规则基于 Andrej Karpathy 的 LLM Wiki 方法论：
> https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f

---

## 核心原则

1. **`inbox/` 是暂存区**：我收藏但尚未整理的文档放在这里，可随意修改、重命名、删除。它**不是**原始资料，未经整理不进入知识图谱。
2. **`raw/` 不可变**：原始资料一旦存入，永远不修改、不删除，只追加。
3. **`wiki/` 可重生**：所有派生内容由你生成和维护，可以随时更新、重新编译。
4. **纯 Markdown + wiki-link**：所有页面用标准 Markdown，关联用 `[[页面名]]`，知识图谱自然形成。
5. **操作留痕**：每次操作后更新 `wiki/_index.md` 和 `wiki/_log.md`。
6. **人负责 sourcing，LLM 负责记账**：我找资料和提问，你做摘要、交叉引用、归档、维护。
7. **可视化优先**：生成或更新摘要、对比、概念、综述、归档等派生页面时，只要存在时间、流程、架构、关系、趋势或多维对比，就尽量增加可直接渲染的 SVG 图，而不只用文字和表格。

---

## 目录结构

```
my-wiki/
├── CLAUDE.md              # 本文件（规则）
├── AGENTS.md              # 软链到 CLAUDE.md，兼容多家 CLI
├── README.md              # 使用说明
├── inbox/                 # 暂存区：收藏但未整理（可随意增删改）
│   ├── _inbox.md          # 暂存清单（非 md 文件的状态与来源）
│   ├── YYYY-MM-DD-标题.md
│   └── files/             # 收藏的 PDF / HTML / 图片原件
├── raw/                   # 不可变原始资料
│   ├── YYYY-MM-DD-标题.md
│   └── assets/            # 图片、PDF 等附件
└── wiki/                  # LLM 派生的理解层
    ├── summaries/         # 每篇资料一个摘要页
    ├── entities/          # 人物、组织、产品、技术
    ├── concepts/          # 方法论、架构模式、理论
    ├── comparisons/       # A vs B 对比
    ├── overviews/         # 主题综述
    ├── synthesis/         # 问答归档（我的观点库）
    ├── _index.md          # 全库内容索引
    └── _log.md            # 操作日志
```

**三区的区别**：`inbox/` 可变（草稿箱）→ `raw/` 不可变（原始快照）→ `wiki/` 可重生（理解层）。

---

## 三大操作

### 1. 摄取（Ingest）

当我给你一个 URL、文件路径或直接粘贴文本时，按以下步骤执行：

1. **保存原始资料**：抓取/读取内容，保存到 `raw/YYYY-MM-DD-标题.md`。
   - 如果是网页，保留标题、URL、抓取日期、正文。
   - 如果是文件，保留文件名和元信息。
   - 图片/附件存入 `raw/assets/`。
2. **生成摘要页**：在 `wiki/summaries/` 创建对应摘要，包含核心观点、关键数据、与已有知识的关联。
3. **提取实体**：识别文中的人物、组织、产品、技术术语 → 更新或创建 `wiki/entities/` 下的页面。
4. **提取概念**：识别方法论、架构模式、理论 → 当某概念被 2 篇以上资料提及时，在 `wiki/concepts/` 建立页面。
5. **建立对比**：当新资料与已有内容形成 A vs B 关系时，在 `wiki/comparisons/` 创建对比页。
6. **更新综述**：当某主题已有 2 篇以上资料时，更新 `wiki/overviews/` 下的主题综述。
7. **更新索引和日志**：更新 `wiki/_index.md`，在 `wiki/_log.md` 追加本次操作记录。

**一次摄取通常触碰 10-15 个页面，这是正常的。**

#### 从暂存区摄取

当我说「收录 inbox/xxx」或「整理暂存区」时：

1. 读取 `inbox/` 中指定文件（或全部 `status: inbox` 的待整理项）。
2. 按上述摄取流程执行：内容存入 `raw/`（原件附件存 `raw/assets/`）→ 生成摘要 → 提取实体/概念 → 更新索引和日志。
3. **不要删除暂存区原件**：把该 md 的 frontmatter `status` 改为 `archived`；非 md 文件在 `inbox/_inbox.md` 中把状态改为 `archived`。由我确认整理质量后手动清理。
4. 暂存区文件本身可以修改（它不受 `raw/` 不可变规则约束）。

### 2. 查询（Query）

当我提问时：

1. 先读 `wiki/_index.md`，定位相关页面。
2. 读取相关页面内容，综合回答。
3. 如果答案有长期价值（如系统性总结、个人观点沉淀），主动建议归档到 `wiki/synthesis/`，经我确认后写入。

### 3. 健康检查（Lint）

当我说「lint wiki」或「检查知识库」时：

- 扫描断链（`[[页面名]]` 指向不存在的页面）
- 扫描孤立页面（没有任何入链的页面）
- 扫描矛盾描述（同一实体/概念在不同页面有冲突说法）
- 扫描过时信息（标注了日期但明显过期的内容）
- 给出修复建议，并在我确认后自动修复

---

## 建页规则

### 页面 frontmatter 规范

每个 wiki 页面顶部必须包含：

```yaml
---
type: summary | entity | concept | comparison | overview | synthesis
title: 页面标题
sources:
  - raw/2026-08-26-某文章.md
created: 2026-08-26
updated: 2026-08-26
tags: [标签1, 标签2]
---
```

### 各类页面要点

- **摘要页（summaries）**：3-5 句话核心观点 + 关键数据 + 关联概念/实体链接。
- **实体页（entities）**：首次提到就建立；包含定义、相关资料列表、在各资料中的角色。
- **概念页（concepts）**：2 篇以上资料提及才建立；包含定义、起源、演进、应用场景、争议。
- **对比页（comparisons）**：2 篇以上资料涉及同一对比才建立；用表格呈现维度对比。
- **综述页（overviews）**：某主题积累 2+ 篇后建立；定期更新，反映该主题的全貌。
- **归档页（synthesis）**：高质量问答的沉淀；是"我的观点库"，区别于客观摘要。

### 派生层报告可读性硬规则

所有摘要报告、分析报告等派生层内容（含 `wiki/` 下各页面及回答提问时生成的报告）必须遵守：

1. **零上下文可读**：报告必须让一个中等智力、但不了解上下文的人类可以看懂。写作时默认读者没有读过原始资料、不了解讨论背景，必要的来龙去脉要在报告内交代清楚。
2. **术语首现即解释**：报告中出现任何缩写、英文缩写、平台名、指标名、业务口径或不常用术语时，必须在首次出现处解释含义，必要时补充一句业务背景。同一报告内后续出现不必重复解释。
   - 示例：`ARPU（Average Revenue Per User，每用户平均收入，衡量业务变现能力的常用指标）`。

### SVG 可视化规范

生成或更新 `wiki/` 中的派生页面时，先判断内容是否存在适合图形表达的结构；有明显增益时，应创建 SVG，而不是只写 Markdown 表格或嵌入 HTML renderer。

- **优先适用页面**：`summaries/`、`comparisons/`、`concepts/`、`overviews/`、`synthesis/`。实体页在关系复杂或存在演进过程时也可增加。
- **图形选型**：
  - 时间演进、调用顺序、阶段变化 → 时序图或时间线；
  - 操作步骤、因果链、状态迁移 → 流程图；
  - 系统分层、组件连接、数据流 → 架构图；
  - 连续数值变化 → 折线图；
  - 多对象、多维度差异 → 对比图、矩阵或分组条形图；
  - 比例、构成、层级和关系 → 选择能准确表达结构的 SVG 图形。
- **事实边界**：图中的数值、时间、节点、连线和因果必须来自原始资料或可复核计算；区分“已公布”“预测”“估算”和“示意”，不得为了画图补造数据。
- **文件与引用**：SVG 文件统一存放在 `wiki/assets/`，使用稳定、语义清晰的英文文件名；Markdown 页面使用标准图片语法引用，例如 `![图示说明](../assets/example.svg)`。不要依赖 HTML、JavaScript、外部 CDN 或特定 renderer。
- **内容要求**：每张图应有明确标题、阅读方向、必要图例、数据口径或来源说明；图中文字保持精简，详细论证仍写在正文中。
- **可读性**：SVG 必须设置 `viewBox`，使用高对比度颜色与通用中文字体回退，避免文字溢出、元素重叠或只靠颜色传达含义；在窄屏缩放后仍应可辨认。
- **克制原则**：图形必须增加理解，不为装饰而画；没有可靠数据或清晰结构时保留文字说明，不强行生成图表。
- **交付检查**：写入后验证 SVG 是有效 XML、Markdown 相对路径可达、实际渲染无乱码/裁切，并确认页面不残留无法渲染的 HTML 代码。

---

## 交互约定

- 我用自然语言下指令，你直接执行，不需要反复确认（除非操作涉及删除或大规模重构）。
- 执行完后简要告诉我：做了什么、新建/更新了哪些页面、有什么值得注意的发现。
- 如果资料内容与已有 wiki 存在矛盾，主动标注出来，不要静默覆盖。
- 不确定某个信息该归到哪个分类时，优先创建摘要页，后续再提炼。
